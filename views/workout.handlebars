<form class="box">
    <div class="field">
        <label class="label has-text-centered is-2">{{name}}</label>
        <div class="control">
            <p>1{{description}}</p>
        </div>
        <div class="control">
            <p>2{{has_interval}}</p>
        </div>
        <div class="control">
            <p>3{{has_repetitions}}</p>
        </div>
        <div class="control">
            <p>4{{intensity}}</p>
        </div>
        <div class="control">
            <p>5{{equipment_required}}</p>
        </div>
    </div>

</form>
